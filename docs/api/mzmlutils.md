# mzmlutils

Read and sample spectra from mzML files. Provides a single-pass proportion
sampler that reads the file in configurable chunks and draws a random
fraction from each buffer, with output to MGF or mzML format via
`casanovoutils mzmlutils`.

```{eval-rst}
.. automodule:: casanovoutils.mzmlutils
   :members:
   :undoc-members:
   :show-inheritance:
```
